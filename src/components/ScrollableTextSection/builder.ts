import { ScrollableTextSectionMetadata } from "../registry";

export const ScrollableTextSectionBuilder: ScrollableTextSectionMetadata = {
  bgColor: "#242526",
  textColor: "#FAF7C3",
  duration: 10,
  direction: "ltr",
};
